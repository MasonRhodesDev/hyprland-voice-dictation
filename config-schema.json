{
  "version": "1.0",
  "title": "Voice Dictation Configuration",
  "description": "Configuration for voice dictation system",
  "sections": [
    {
      "id": "daemon",
      "title": "Daemon",
      "description": "Daemon engine settings",
      "fields": [
        {
          "id": "audio_device",
          "label": "Audio Device",
          "description": "ALSA audio input device name (e.g., 'default', 'pipewire', 'sysdefault:CARD=Pro'). Run 'arecord -L' to see all available devices.",
          "type": "enum",
          "options_source": {
            "type": "script",
            "command": "voice-dictation list-audio-devices"
          },
          "default": "default",
          "ui_widget": "dropdown"
        },
        {
          "id": "sample_rate",
          "label": "Sample Rate",
          "description": "Audio sample rate in Hz",
          "type": "enum",
          "options_source": {
            "type": "static",
            "values": ["8000", "16000", "22050", "44100", "48000"]
          },
          "default": "16000",
          "ui_widget": "dropdown"
        },
        {
          "id": "model",
          "label": "Model",
          "description": "Transcription model (Parakeet).",
          "type": "enum",
          "options_source": {
            "type": "script",
            "command": "voice-dictation list-models",
            "cache_duration": 3600
          },
          "default": "parakeet:default",
          "ui_widget": "dropdown"
        },
        {
          "id": "enable_acronyms",
          "label": "Acronym Detection",
          "description": "Convert letter-by-letter patterns to acronyms (e.g., 'a p i' → 'API'). ~2ms overhead.",
          "type": "boolean",
          "default": true,
          "ui_widget": "toggle"
        },
        {
          "id": "enable_punctuation",
          "label": "Capitalization",
          "description": "Capitalize first word, pronoun 'I', and after sentence endings. ~5ms overhead.",
          "type": "boolean",
          "default": true,
          "ui_widget": "toggle"
        },
        {
          "id": "enable_grammar",
          "label": "Grammar & Spell Checking",
          "description": "Use Harper for grammar and spelling corrections (developer-friendly). ~10-20ms overhead.",
          "type": "boolean",
          "default": true,
          "ui_widget": "toggle"
        },
        {
          "id": "enable_word_substitution",
          "label": "Word Substitution",
          "description": "Replace spoken phrases with desired text (e.g., 'shay moy' → 'chezmoi'). Edit ~/.local/share/voice-dictation/substitutions.txt to configure.",
          "type": "boolean",
          "default": true,
          "ui_widget": "toggle"
        },
        {
          "id": "trailing_buffer_ms",
          "label": "Trailing Audio Buffer",
          "description": "Duration in milliseconds to continue recording after stop command (captures final words that might otherwise be cut off)",
          "type": "number",
          "default": 750,
          "min": 0,
          "max": 2000,
          "ui_widget": "number_input"
        },
        {
          "id": "media_resume_delay_ms",
          "label": "Media Resume Delay",
          "description": "Delay in milliseconds before resuming paused media after recording stops",
          "type": "number",
          "default": 25,
          "min": 0,
          "max": 2000,
          "ui_widget": "number_input"
        },
        {
          "id": "audio_backend",
          "label": "Audio Backend",
          "description": "Audio capture backend: 'auto' (prefer PipeWire, fallback to cpal), 'pipewire' (native PipeWire for mic sharing), 'cpal' (cross-platform via ALSA).",
          "type": "enum",
          "options_source": {
            "type": "static",
            "values": ["auto", "pipewire", "cpal"]
          },
          "default": "auto",
          "ui_widget": "dropdown"
        },
        {
          "id": "idle_release_timeout_secs",
          "label": "Idle Release Timeout",
          "description": "Seconds to keep microphone open after recording stops (allows fast re-start). After timeout, mic is released for other apps. Set to 0 for immediate release.",
          "type": "number",
          "default": 30,
          "min": 0,
          "max": 300,
          "ui_widget": "number_input"
        },
        {
          "id": "enable_correction_learning",
          "label": "Correction Learning",
          "description": "Automatically detect when you correct transcribed text and learn from corrections. Requires AT-SPI2 accessibility bus (available by default on GNOME/KDE). ~1-2ms per injection (background monitoring).",
          "type": "boolean",
          "default": true,
          "ui_widget": "toggle"
        },
        {
          "id": "correction_monitor_duration_secs",
          "label": "Correction Monitor Duration",
          "description": "How many seconds to monitor for corrections after text injection.",
          "type": "number",
          "default": 60,
          "min": 10,
          "max": 300,
          "ui_widget": "number_input"
        },
        {
          "id": "correction_auto_promote_threshold",
          "label": "Auto-Promote Threshold",
          "description": "Number of times a correction must be seen before it's automatically added as a word substitution rule.",
          "type": "number",
          "default": 3,
          "min": 1,
          "max": 20,
          "ui_widget": "number_input"
        },
        {
          "id": "enable_accessibility_bridge",
          "label": "Enable Accessibility Bridge",
          "description": "Automatically enable the GTK/Qt accessibility bridge on daemon startup. Required for correction learning to detect text edits. Sets org.gnome.desktop.interface toolkit-accessibility to true via gsettings.",
          "type": "boolean",
          "default": true,
          "ui_widget": "toggle"
        }
      ]
    }
  ]
}
